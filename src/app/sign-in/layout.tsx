import RegistrationView from "@/components/RegistrationPage";
import React from "react";

export default ({ children }: { children: React.ReactNode }) => {
  return <RegistrationView>{children}</RegistrationView>;
};
